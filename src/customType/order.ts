export type Order = {
  count: number;
  hasReview: boolean;
  orderedAt: number;
  productName: string;
  productId: string;
  productImage: string;
  totalPrice: number;
  userId: string;
};
