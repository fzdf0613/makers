export type Order = {
  id: string;
  count: number;
  hasReview: boolean;
  orderedAt: number;
  productName: string;
  productId: string;
  productImage: string;
  productPrice: number;
  totalPrice: number;
  userId: string;
};
