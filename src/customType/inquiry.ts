export type Inquiry = {
  answer: string;
  createdAt: number;
  id: string;
  productId: string;
  productImage: string;
  productName: string;
  text: string;
  userId: string;
  username: string;
};
